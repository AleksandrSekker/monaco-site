// Import all schema files
import hero from './hero';
import service from './service';

// Define schemas array
const schemas = [hero, service];

// Export all schemas as an array
export default schemas;

